name: Oracle A1 VM Auto Retry
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  create-vm:
    runs-on: ubuntu-latest
    steps:
      - name: Install OCI CLI
        run: pip install oci-cli --quiet

      - name: Configure OCI
        run: |
          mkdir -p ~/.oci ~/.ssh
          echo "[DEFAULT]" > ~/.oci/config
          echo "user=${{ secrets.USER_OCID }}" >> ~/.oci/config
          echo "fingerprint=${{ secrets.OCI_FINGERPRINT }}" >> ~/.oci/config
          echo "tenancy=${{ secrets.TENANCY_OCID }}" >> ~/.oci/config
          echo "region=${{ secrets.OCI_REGION }}" >> ~/.oci/config
          echo "key_file=/home/runner/.oci/key.pem" >> ~/.oci/config
          printf '%s' "${{ secrets.OCI_PRIVATE_KEY }}" > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem ~/.oci/config
          ssh-keygen -t rsa -b 2048 -f ~/.ssh/id_rsa -N "" -q
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > ~/.ssh/id_rsa.pub

      - name: Test OCI Connection
        run: oci iam region list --output table

      - name: Try Create VM
        run: |
          for AD in 1 2 3; do
            echo "=== Trying AD-$AD ==="
            RESULT=$(oci compute instance launch \
              --compartment-id ${{ secrets.TENANCY_OCID }} \
              --availability-domain "hWQW:US-ASHBURN-AD-$AD" \
              --display-name "Roofing-Machine" \
              --image-id ocid1.image.oc1.iad.aaaaaaaahk7zm5s5gv4imyzgui5c77uzfpn72blj3od2icmohxzv3zpuiikq \
              --shape "VM.Standard.A1.Flex" \
              --shape-config '{"ocpus":4,"memoryInGBs":24}' \
              --subnet-id ocid1.subnet.oc1.iad.aaaaaaaaiobhami2yqammfaswb7r3biry6wjst7kvfilimz53qmxopsr6o4q \
              --assign-public-ip true \
              --ssh-authorized-keys-file ~/.ssh/id_rsa.pub 2>&1 || true)
            echo "$RESULT"
            if echo "$RESULT" | grep -q "lifecycle-state"; then
              echo "SUCCESS on AD-$AD!"
              exit 0
            fi
            echo "AD-$AD failed - capacity nahi"
          done
          echo "All ADs failed - retry in 15 min automatically"

      - name: Add Cloud Shell Key to Existing Instance
        run: |
          cat > /tmp/add_key.sh << 'SCRIPT'
          #!/bin/bash
          echo "$SSH_PUB" >> /home/ubuntu/.ssh/authorized_keys
          chmod 600 /home/ubuntu/.ssh/authorized_keys
          SCRIPT
          oci compute instance run-command \
            --instance-id ocid1.instance.oc1.iad.anuwcljswpc6fdacejrnc7tpxs2z362f7zahh54pdhmu2epgtj7gzlw7qa4a \
            --compartment-id ${{ secrets.TENANCY_OCID }} \
            --run-command-script-file /tmp/add_key.sh \
            --execution-time-out-in-seconds 60 \
            --wait-for-state SUCCEEDED \
            --parameters "[{\"name\":\"SSH_PUB\",\"value\":\"${{ secrets.SSH_PUBLIC_KEY }}\"}]"
